#include "main.h"

/**
 * _abs - calculates absolute value of number
 * @n: the number we need to calculate
 *
 * Return: abs of number
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
